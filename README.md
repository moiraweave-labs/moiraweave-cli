# MoiraWeave CLI

Developer CLI for the MoiraWeave platform.

## Install

```bash
uv tool install git+https://github.com/moiraweave-labs/moiraweave-cli
```

## Usage

```bash
moira --help
```

The CLI covers initialization, task discovery, step management, pipeline validation, and job inspection.
